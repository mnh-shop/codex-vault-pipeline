"""Graph projection — data layer.

This package provides reusable modules for reading source metadata
and generating Obsidian graph artifacts from vault runtime data.
All modules are read-only on vault data and write only to
``wiki/_graph/`` when explicitly invoked.
"""
